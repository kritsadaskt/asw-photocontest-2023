import React from "react";

export default class imageListed extends React.Component {
  logThis(x) {
    console.log(x);
  }
  componentDidMount() {
    this.logThis('hehe');
  }
  render() {
    return 
  }
}